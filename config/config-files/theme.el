;; Chargement du theme
(load-theme 'nord t)

;; Enlève la barre de défilement
(scroll-bar-mode -1)

;; Tool bar
(tool-bar-mode -1)

;;Nlinum

;;format
(setq nlinum-format "%5d")
(setq nlinum-highlight-current-line t)
